services:
  colibri:
    build:
      context: .
      dockerfile: deploy/colibri/Dockerfile
    image: leo-ai-colibri:1.11.0
    environment:
      COLI_RAM: "${COLIBRI_RAM:-24}"
    ports:
      - "127.0.0.1:${COLIBRI_PORT:-8081}:5000"
    volumes:
      - "${COLIBRI_MODEL_DIR:?define COLIBRI_MODEL_DIR con la ruta del modelo}:/model:ro"
    command:
      - serve
      - --host
      - 0.0.0.0
      - --model-id
      - "${COLIBRI_MODEL_ID:-glm-5.2}"
      - --ram
      - "${COLIBRI_RAM:-24}"
    shm_size: "2gb"
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: leo
      POSTGRES_PASSWORD: leo
      POSTGRES_DB: leo
    ports:
      - "5439:5432"
    volumes:
      - leo-pg:/var/lib/postgresql/data
      - ./deploy/postgres/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U leo -d leo"]
      interval: 2s
      timeout: 5s
      retries: 20

volumes:
  leo-pg:
